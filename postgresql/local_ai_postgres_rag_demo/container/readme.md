Start the container using Docker Compose:
```bash
docker compose up -d --build
```

Extra
```
docker compose ps

docker compose logs -f

docker compose down --volumes
```

Stop and remove the container, network, and resources:
```bash
docker compose down
```
